# requirements.txt
# Specify an extra index URL for PyTorch's CUDA-specific wheels
--extra-index-url https://download.pytorch.org/whl/cu121

# PyTorch and related libraries (ensure these versions match the cu121 index)
torch==2.3.1+cu121
torchvision==0.18.1+cu121
torchaudio==2.3.1+cu121

# Other libraries
transformers
accelerate
jupyter
ipykernel
